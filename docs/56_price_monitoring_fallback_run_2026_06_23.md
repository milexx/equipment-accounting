# Запуск Резервной Цепочки Оценщика 2026-06-23

Дата: 2026-06-23
Идентификатор запуска: `20260623T052144Z`

## Область Применения

Контролируемый ручной запуск резервной цепочки оценщика:

```text
Avito -> Duff89/parser_avito -> Юла
```

Планировщик не включался. Запуск был выполнен один раз за UTC-день.

## Предварительная Проверка

Preflight вернул статус `ready`:

- UTC-дата: `2026-06-23`;
- боевой запуск за этот UTC-день ранее не найден;
- конфигурация валидна;
- позиций в конфигурации: 3;
- диагностика блокировок включена;
- fallback на Юлу включен.

## Результат

Итоговый статус запуска: `success`

| Позиция | Итоговый источник | HTTP | Статус | Релевантные | Неопределенные | Отклоненные | Минимум | Максимум | Медиана |
| --- | --- | ---: | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `kyocera_m2040dn` | `avito` | 200 | `success` | 39 | 5 | 6 | 14 000 | 78 000 | 24 999 |
| `lenovo_t14` | `avito` | 200 | `success` | 26 | 0 | 24 | 16 990 | 53 900 | 29 750 |
| `dell_r740` | `avito` | 200 | `success` | 35 | 1 | 14 | 87 000 | 712 000 | 153 400 |

## Поведение Источников

- Основной обработчик Avito сработал по всем трем позициям.
- `Duff89/parser_avito` не запускался как итоговый fallback, потому что основной источник вернул успешные данные.
- Юла не понадобилась.
- Блокировок, CAPTCHA и ошибок парсера в этом запуске не было.

## Импорт В Базу

Запуск импортирован в таблицы оценщика:

```text
run_id=20260623T052144Z
status=success
observations_created=100
snapshots_saved=3
parser_errors_created=0
```

В базе сохранены три дневных снимка с фактическим источником `avito`:

- `kyocera_m2040dn`: медиана 24 999;
- `lenovo_t14`: медиана 29 750;
- `dell_r740`: медиана 153 400.

## Вывод

Решение: `keep_fallback_chain_for_mvp_manual`

Обоснование:

- дневной запуск дал пригодные снимки по всем отслеживаемым позициям;
- основной Avito в этот день не был заблокирован;
- история оценщика пополнена реальными ценовыми точками без записи `blocked` как результата оценки.

Ограничения остаются:

- только ручные запуски;
- без планировщика;
- без цикла повторов после блокировки;
- без прокси, cookies и обхода CAPTCHA;
- один контролируемый боевой запуск в UTC-день, если нет явного разрешения на исключение.
