# Запуск Резервной Цепочки Оценщика 2026-06-24

Дата документа: 2026-06-24
Идентификатор запуска: `20260625T052341Z`

Ключевой вывод: контролируемый ручной прогон прошел штатно по основному `Avito` для всех трех позиций. Fallback-цепочка `Duff89/parser_avito -> Юла` в этом запуске не понадобилась. Важно: локально запуск был инициирован 2026-06-24 (`America/Phoenix`), но воркер ведет guard и `run_id` по UTC, поэтому фактическая дата запуска в артефактах: `2026-06-25 UTC`.

## Область Применения

Контролируемый ручной запуск резервной цепочки оценщика:

```text
Avito -> Duff89/parser_avito -> Юла
```

Планировщик не включался. Запуск был выполнен один раз за UTC-день.

## Предварительная Проверка

Preflight вернул статус `ready`:

- UTC-дата воркера: `2026-06-25`;
- боевой запуск за этот UTC-день ранее не найден;
- конфигурация валидна;
- позиций в конфигурации: 3;
- диагностика блокировок включена;
- fallback на Юлу включен.

## Результат

Итоговый статус запуска: `success`

| Позиция | Итоговый источник | HTTP | Статус | Релевантные | Неопределенные | Отклоненные | Минимум | Максимум | Медиана |
| --- | --- | ---: | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `kyocera_m2040dn` | `avito` | 200 | `success` | 41 | 4 | 5 | 14 000 | 78 000 | 24 000 |
| `lenovo_t14` | `avito` | 200 | `success` | 32 | 0 | 18 | 16 990 | 70 000 | 27 745 |
| `dell_r740` | `avito` | 200 | `success` | 34 | 2 | 14 | 87 000 | 712 000 | 139 900 |

## Поведение Источников

- Основной обработчик `Avito` сработал по всем трем позициям.
- `Duff89/parser_avito` не запускался как итоговый fallback, потому что основной источник вернул успешные данные.
- Юла не понадобилась.
- Блокировок, CAPTCHA и ошибок парсера в этом запуске не было.

## Импорт В Базу

Запуск импортирован в таблицы оценщика:

```text
run_id=20260625T052341Z
status=success
observations_created=107
snapshots_saved=3
parser_errors_created=0
```

В базе сохранены три дневных снимка с фактическим источником `avito`:

- `kyocera_m2040dn`: медиана 24 000;
- `lenovo_t14`: медиана 27 745;
- `dell_r740`: медиана 139 900.

## Проверка Интерфейса

Проверка локального сервиса подтвердила, что маршрут `/pricing` отвечает:

- `GET http://127.0.0.1:8010/pricing` -> `200`;
- импортированный запуск доступен для UI модуля оценщика.

Подробный офлайн-отчет сохранен в [offline_report.md](/opt/workspace/projects/equipment-accounting/research/avito-monitor-worker-poc/runs/20260625T052341Z/offline_report.md).

## Вывод

Решение: `keep_fallback_chain_for_mvp_manual`

Обоснование:

- дневной запуск дал пригодные снимки по всем отслеживаемым позициям;
- основной `Avito` в этот день не был заблокирован;
- fallback-цепочка осталась доступным резервом, но не понадобилась;
- история оценщика пополнена реальными ценовыми точками без записи `blocked` как результата оценки.

Ограничения остаются:

- только ручные запуски;
- без планировщика;
- без цикла повторов после блокировки;
- без прокси, cookies и обхода CAPTCHA;
- один контролируемый боевой запуск в UTC-день, если нет явного разрешения на исключение.
